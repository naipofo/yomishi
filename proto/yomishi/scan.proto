syntax = "proto3";

package yomishi.scan;

service Scan {
  rpc ScanString(ScanStringRequest) returns (ScanStringReply) {}
}

message ScanStringRequest { string text = 1; }

message ScanStringReply { repeated string results = 1; }